/*
Deployment script for Applebrie-db

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "Applebrie-db"
:setvar DefaultFilePrefix "Applebrie-db"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Altering Procedure [users].[CreateUser]...';


GO
ALTER PROCEDURE [users].[CreateUser]
	@name   nvarchar(25),
	@sex    bit,
	@role  tinyint,
	@status tinyint
as
	begin
		insert into [users].[Users]([Name], [Sex], [Role], [Status], [Created], [Updated])
			values(LTRIM(rtrim(@name)), @sex, @role, @status, GETUTCDATE(), GETUTCDATE())
	
		SELECT TOP 1 * FROM [users].[Users] ORDER BY ID DESC
				FOR JSON PATH, WITHOUT_ARRAY_WRAPPER
	end;
GO
PRINT N'Altering Procedure [users].[UpsertUsers]...';


GO
ALTER PROCEDURE [users].[UpsertUsers]	
	@id     int = null,
	@name   nvarchar(25),
	@sex    bit,
	@role   tinyint,
	@status tinyint
as
begin
	if(ISNULL(@id, 0) = 0)

		begin
			insert into [users].[Users]([Name], [Sex], [Role], [Status], [Created], [Updated])
				values(LTRIM(rtrim(@name)), @sex, @role, @status, GETUTCDATE(), GETUTCDATE())
	
			SELECT TOP 1 * FROM [users].[Users] ORDER BY ID DESC
					FOR JSON PATH, WITHOUT_ARRAY_WRAPPER
		end
	else
		begin
			update u
				set [Name] = LTRIM(rtrim(@name)),
				[Sex] = @sex,
				[Role] = @role,
				[Status] = @status,
				[Updated] = GETUTCDATE()
					from [users].[Users] as u
				where [id] = @id

			SELECT * FROM [users].[Users] where [id] = @id
					FOR JSON PATH, WITHOUT_ARRAY_WRAPPER

		end
end;
GO
PRINT N'Update complete.';


GO
