using System;
using System.Collections.Generic;
using System.Text;

namespace Applebrie.Common.Enums
{
    public enum Sex
    {
        Male = 0,
        Female = 1
    }

    public enum Status
    {
        Active = 1,
        Deleted = 2,
        Blocked = 3
    }

    public enum Roles
    {
        Admin = 1,
        User = 2,
        Other = 3
    }
}
